import styled from 'styled-components/native';

export const NormalText = styled.Text`
  font-weight: 400;
  font-size: 14px;
  line-height: 20px;

  color: #1c1c1e;
`;

// Title Onboarding
export const Title = styled.Text`
  font-weight: 700;
  font-size: 28px;
  line-height: 34px;
  color: #ffffff;
`;

export const Title16 = styled.Text`
  font-weight: 700;
  font-size: 16px;
  line-height: 24px;

  color: #ffffff;
`;

export const BoldValue = styled.Text`
  font-weight: 700;
  font-size: 32px;
  line-height: 44px;

  color: #ffffff;
`;
